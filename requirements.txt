Pillow>=10.0
lunardate>=0.2
lunar_python>=1.4
flask>=2.0
APScheduler>=3.10
# 说明：
# - main.py 直推（Image API）只需 Pillow/lunardate/lunar_python
# - server.py 托管推送服务（多用户注册 + 定时批量推图）需要额外 flask + APScheduler
# - rss_server.py RSS 服务需要 flask
